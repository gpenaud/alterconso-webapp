apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Chart.Name }}
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "{{ .Values.pv.host_path }}"
