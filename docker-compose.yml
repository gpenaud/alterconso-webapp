version: "3.2"

networks:
  cagette:

volumes:
  database:

services:
  cagette:
    image: gpenaud/cagette
    build:
      context: .
      args:
        CAGETTE_SMTP_USER: "${CAGETTE_SMTP_USER:-}"
        CAGETTE_SMTP_PASSWORD: "${CAGETTE_SMTP_PASSWORD:-}"
        CAGETTE_SQL_LOG: "${CAGETTE_SQL_LOG:-}"
        CAGETTE_DEBUG: "${CAGETTE_DEBUG:-}"
    # for debugging purposes
    volumes:
      - type: bind
        source: ./docker/app/src/controller/Install.hx
        target: /var/www/cagette/src/controller/Install.hx
    ports:
      - 80:80
      - 443:443
    depends_on:
      - mysql
    networks:
      - cagette

  crons:
    image: gpenaud/cagette
    entrypoint: sh /var/www/cagette/crontab.sh
    depends_on:
      - cagette

  mysql:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: db
      MYSQL_USER: docker
      MYSQL_PASSWORD: docker
    ports:
      - 3306
    volumes:
      - database:/var/lib/mysql
      - ./docker/mysql/dumps/:/root/dumps
      - ./docker/mysql/my.cnf:/etc/mysql/conf.d/my.cnf
    networks:
      - cagette
